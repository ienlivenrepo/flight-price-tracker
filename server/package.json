{
  "name": "server",
  "version": "1.0.0",
  "description": "api to find flight prices for a given route",
  "main": "src/server.ts",
  "scripts": {
    "build": "tsc",
    "prestart": "npm run build",
    "dev": "ts-node-dev --respawn --transpileOnly src/server.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "indrajit singh",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^3.6.2"
  },
  "dependencies": {
    "@types/express": "^4.17.1",
    "axios": "^0.19.0",
    "express": "^4.17.1",
    "lodash": "^4.17.15",
    "ts-node-dev": "^1.0.0-pre.42",
    "typescript-map": "0.0.7",
    "unirest": "^0.6.0"
  }
}
